# Nhom-5-CNPM

## G5PSC-18 — Frontend foundation

Khung giao diện React + Vite gồm Material UI Theme, Layout responsive,
React Router, AuthContext mock, ProtectedRoute, RoleRoute và Sidebar phân
quyền cho ADMIN, TEACHER, STUDENT, PARENT.

```bash
npm install
npm run dev
```

Kiểm tra production build:

```bash
npm run build
```
